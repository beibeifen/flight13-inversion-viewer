# Optional runtime data

The public repository intentionally contains no broadcast-derived HUD frames. A locally built
`source-hud.zip` is ignored by Git and remains outside the repository license. The server can
read it directly without extracting thousands of files.
